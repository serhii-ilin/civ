# Baseline Artifacts

Generated outputs for `baseline-v0.1` belong in this directory.

Use the structure defined in `canon/artifact-structure.md`, relative to the current agent folder. The current files are placeholders for the artifact root; generated research output should create the required `research/`, `architecture/`, `simulation/`, `implementation/`, and `review/` subdirectories.
